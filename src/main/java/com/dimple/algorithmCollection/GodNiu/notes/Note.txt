1、时间复杂度

    常数时间的操作：一个操作如果和数据量没有关系，每次都是在固定时间内完成的操作。比如加减乘除运算，数组寻址；

    时间复杂度是为一个算法流程中的，常数操作数量的指标。常用O表示。
        具体来说：在常数操作数量的表达式中，只要高阶项，不要低阶项，不要高阶项的系数，剩下的部分即为f(N),那么时间复杂度为O(f(N))
2、递归（递归栈）-任何递归行为改为迭代的方式
    一个函数在进行递归调用之前，会将当前函数的状态压入栈中，当子过程返回后，会将这个压入栈中的函数彻底还原，继续跑。